# Copy to .env and add your values (loaded on deploy)
# Postmark: Get token from Postmark > Server > API Tokens
# Use a verified sender from Postmark > Sender Signatures

POSTMARK_API_TOKEN=your_postmark_server_token
POSTMARK_FROM=Newphase Coaching <notifications@yourdomain.com>
